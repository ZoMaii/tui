{
    "contentHtml": "<p>Node.js 性能优化需要从<strong>事件循环、内存管理和进程模型</strong>三个维度综合考量。</p><h2 id=\"toc-eventloop\">理解事件循环</h2><p>Node.js 的事件循环分为多个阶段：<strong>timers、pending callbacks、idle/prepare、poll、check、close callbacks</strong>。理解各阶段有助于避免阻塞。</p><h2 id=\"toc-cluster\">集群模式</h2><p>利用 <code>cluster</code> 模块可以<strong>充分利用多核 CPU</strong>，主进程负责分发请求，工作进程处理实际业务。</p><div class=\"code-block\"><pre><code class=\"language-javascript\">const cluster = require('cluster');\nconst os = require('os');\nconst http = require('http');\n\nif (cluster.isPrimary) {\n  const numCPUs = os.cpus().length;\n  for (let i = 0; i < numCPUs; i++) {\n    cluster.fork();\n  }\n  cluster.on('exit', (worker) => {\n    console.log(`Worker ${worker.id} 退出，正在重启...`);\n    cluster.fork();\n  });\n} else {\n  http.createServer((req, res) => {\n    res.end('Hello from worker ' + cluster.worker.id);\n  }).listen(3000);\n}</code></pre></div><h2 id=\"toc-memory\">内存管理</h2><p>注意<strong>内存泄漏</strong>的常见来源：全局变量、未清理的定时器、闭包引用、以及事件监听器未移除。使用 <code>--inspect</code> 和 Chrome DevTools 进行 heap snapshot 分析。</p><h2 id=\"toc-summary\">总结</h2><p>生产环境的 Node.js 优化需要<strong>持续监控</strong>（如使用 PM2、Prometheus），结合集群模式、合理的内存管理和事件循环优化。</p>"
}