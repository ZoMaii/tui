{
    "contentHtml": "<p>React 19 是 React 生态系统的一次重大升级，带来了<strong>全新的 React Compiler</strong>（自动记忆化）、改进的<strong>服务端组件</strong>支持，以及多个开发者体验优化。</p><h2 id=\"toc-compiler\">React Compiler — 自动记忆化</h2><p>React 19 引入了一个实验性的编译器，它能在构建时<strong>自动为代码添加 memo、useMemo 和 useCallback</strong>。开发者不再需要手动管理这些优化，编译器会分析组件的依赖关系并自动处理重新渲染。</p><div class=\"code-block\"><pre><code class=\"language-jsx\">// React 19: 编译器自动优化，无需手动 memo\nfunction UserProfile({ user }) {\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;{user.name}&lt;/h2&gt;\n      &lt;p&gt;{user.email}&lt;/p&gt;\n    &lt;/div&gt;\n  );\n}\n// 编译器自动等价于 React.memo(UserProfile)</code></pre></div><h2 id=\"toc-use-hook\">全新的 use() Hook</h2><p><code>use()</code> 是 React 19 中一个突破性的 Hook，它允许在渲染期间<strong>直接读取 Promise 和 Context</strong>，而不需要配合 Suspense 的繁琐写法。</p><div class=\"code-block\"><pre><code class=\"language-jsx\">async function fetchUser(id) {\n  const res = await fetch(`/api/users/${id}`);\n  return res.json();\n}\n\nfunction UserPage({ userId }) {\n  const user = use(fetchUser(userId));\n  return &lt;div&gt;欢迎, {user.name}&lt;/div&gt;\n}</code></pre></div><blockquote>提示：use() 必须在组件的顶层调用，且它的 Promise 应该被缓存以避免重复请求。</blockquote><h2 id=\"toc-ref-cleanup\">Ref 清理函数</h2><p>React 19 允许在 <code>useRef</code> 的回调形式中返回一个<strong>清理函数</strong>，当元素从 DOM 中移除时自动执行。这对于管理事件监听器和第三方库非常有用。</p><h2 id=\"toc-metadata\">文档元数据支持</h2><p>现在你可以在组件树中的<strong>任意位置</strong>渲染 <code>&lt;title&gt;</code>、<code>&lt;meta&gt;</code> 和 <code>&lt;link&gt;</code> 标签，React 会自动将它们提升到文档头部。这让 SEO 管理和动态标题变得异常简单。</p><h2 id=\"toc-summary\">总结</h2><p>React 19 的核心改进围绕<strong>减少手动优化</strong>和<strong>提升开发者体验</strong>展开。Compiler 自动记忆化、use() Hook、Ref 清理函数和元数据支持是最值得关注的四大特性。</p>"
}